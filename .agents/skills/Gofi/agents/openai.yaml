interface:
  display_name: "Gofi"
  short_description: "Repo-specific patterns and workflows for Gofi"
  default_prompt: "Use the Gofi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true